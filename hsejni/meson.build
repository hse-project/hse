subdir('include')
subdir('java')
subdir('src')
if get_option('tests')
	# Test tries to include <hse/hse_params.h> which doesn't exist.
	# Remove the comment if and when this is ever fixed.
	# subdir('tests')
endif
