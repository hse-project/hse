subdir('include')
subdir('java')
subdir('source')
if get_option('tests')
	# Test tries to include <hse/hse_params.h> which doesn't exist.
	# Remove the comment if and when this is ever fixes.
	# subdir('test')
endif
